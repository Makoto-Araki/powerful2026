# このリポジトリについて
- 個人の Shopify アカウントの開発サイトで公開した Dawn テーマの liquid ファイル群。顧客のコードではない。
- 顧客からのテーマ修正依頼や障害調査依頼を想定した、Claude Code の性能確認用。

# 基本方針
- 顧客のコードを修正する案件を想定し、正は Shopify の編集用(edit)テーマとする。GitHub は変更の記録とレビュー用であり、`main` は Shopify より遅れていることがある。
- Shopify 側の変更を、破棄したり巻き戻したりしない。`git checkout -- .`、`git restore`、`git stash`、`git clean` などで、`theme pull` で展開された内容を消さない。
- 公開中(ライブ)テーマへの反映は、このフローの対象外とする。
- Shopify 上でのテーマ複製(編集用と保存用の作成)は、顧客が行う。

# 業務フロー
1. ユーザーが `main` に切り替え、`shopify theme pull` で Shopify 上の最新の編集用テーマを `main` の作業ツリーに展開する。`git pull` は行わない。
2. ユーザーが自然言語でテーマ調査・テーマ修正を依頼する。
3. Claude Code が開発ブランチを作成して、調査・コード修正を行い、変更内容を報告して止まる。
4. ユーザーが `shopify theme dev` でプレビューを確認する。修正が必要なら 3 に戻る。
5. ユーザーが `shopify theme push` で編集用テーマへ反映して、顧客に確認してもらう。修正が必要なら 3 に戻る。
6. Shopify への反映が確定した後に、ユーザーがコミット・プッシュ・PR作成を依頼し、Claude Code が実施する。これにより、`main` が Shopify の記録になる。
7. ユーザーが GitHub 上で PR をマージし、リモートの開発ブランチを削除する。
8. ユーザーの依頼で、Claude Code がローカルの開発ブランチを削除する。
9. ユーザーが Shopify からログアウトする。

詳細な手順は README.md の「開発手順」を参照する。

# 作業ルール

## 作業開始時
- `git pull` は行わない。ユーザーが `theme pull` で最新の Shopify のテーマを `main` の作業ツリーに展開済みのため、作業ツリーを正として作業を始める。
- `theme pull` で展開された内容と `main` との差分は、確認も報告もせず、破棄もしない。今回の PR に含まれる。
- 現在のブランチが `main` でない場合は、状況を報告してユーザーに確認する。ただし、前回の依頼の PR が未マージで、同じ開発ブランチで作業を続ける場合(プレビュー NG による再修正、PR への追加修正)は、そのブランチで作業する。

## 調査のみの依頼
- コードを変更しない調査では、ブランチを作らず、調査結果の報告で終える。
- 調査は、`theme pull` 後の作業ツリー(Shopify の状態)を正として行う。

## ブランチ
- 修正作業では、編集を始める前に、依頼内容から `feature/英語の短い名前` の開発ブランチを作成する。`theme pull` で展開された未コミットの内容は、そのままブランチに持ち越す。
- 1つの依頼につき、1つのブランチ、1つの PR とする。
- 開発ブランチの削除は、リモートはユーザーが GitHub 上で行う。ローカルはユーザーの依頼があったときに、最新の `main` をプルした後で `git branch -d` で削除し、その後に `git fetch --prune origin` でリモート追跡参照を整理する。
- GitHub 上のマージは squash and merge のため、`git branch -d` では「HEAD にはマージされていない」という警告が出る。これは、リモート追跡ブランチ(`origin/feature/...`)がマージ済みと判定されるため、削除自体は成功する。`git fetch --prune` を先に実行すると追跡参照が消え、`-d` が失敗するので、必ず `-d` を先に実行する。`-D` は使わない。

## Git 操作
- ユーザーが明示的に依頼するまで、コミット・プッシュ・PR作成を行わない。
- ファイルを編集した後は、変更内容の報告で止める。
- ステージングは `git add .` ではなく、ファイルを指定して行う。`theme pull` で展開された内容の差分のファイルも、対象に含める。
- Issue は使わない。コミットメッセージと PR に Issue 番号は付けない。
- コミットメッセージと PR のタイトル・本文は、日本語で変更内容を簡潔に書く。
- コミットの依頼を受けたら、README.md の「開発履歴」に追記する行(日付・バージョン・説明)を提案し、ユーザーの了承を得てから、同じコミットに含める。

## Shopify CLI
- `shopify` コマンド(`auth`、`theme pull`、`theme dev`、`theme push`、`theme delete` など)は実行しない。ユーザーが実行する。
- 調査や修正にストア側の情報(テーマエディタの設定、アプリ、エラー内容、再現 URL など)が必要な場合は、ユーザーに提供を求める。リポジトリのコードだけでは判断できない点は、推測せずその旨を報告する。
