# このリポジトリについて
- 個人の Shopify アカウントの開発サイトで公開した Dawn テーマの liquid ファイル群。顧客のコードではない。
- 顧客からのテーマ修正依頼や障害調査依頼を想定した、Claude Code の性能確認用。

# 業務フロー
1. ユーザーが自然言語でテーマ調査・テーマ修正を依頼する。
2. Claude Code が調査・コード修正を行い、変更内容を報告して止まる。
3. ユーザーが `shopify theme dev` でプレビューを確認する。修正が必要なら 2 に戻る。
4. ユーザーがコミット・プッシュ・PR作成を依頼し、Claude Code が実施する。
5. ユーザーが GitHub 上で PR をマージし、リモートの開発ブランチを削除する。
6. ユーザーの依頼で、Claude Code がローカルの開発ブランチを削除する。

詳細な手順は README.md の「開発手順」を参照する。

# 作業ルール

## 作業開始時
- 作業依頼を受けたら、最初に `main` に切り替えて、GitHub 上でマージ済みの最新の `main` をプルしてから作業を始める。
- ユーザーが作業前に `shopify theme pull` を行っている場合、未コミットの差分が残っていることがある。その差分は破棄せず、存在をユーザーに伝える。今回の変更に含めるかはユーザーに確認する。
- プルが失敗した場合(未コミットの差分との競合など)は、原因を報告して止まる。
- 前回の依頼の PR が未マージで、同じ開発ブランチで作業を続ける場合(プレビュー NG による再修正、PR への追加修正)は、`main` に切り替えず、そのブランチで作業する。

## ブランチ
- 修正作業では、編集を始める前に、依頼内容から `feature/英語の短い名前` の開発ブランチを作成する。
- 1つの依頼につき、1つのブランチ、1つの PR とする。
- 調査のみの依頼(コードを変更しない)では、ブランチを作らず、調査結果の報告で終える。
- 開発ブランチの削除は、リモートはユーザーが GitHub 上で行う。ローカルはユーザーの依頼があったときに、最新の `main` をプルした後で `git branch -d` で削除し、その後に `git fetch --prune origin` でリモート追跡参照を整理する。
- GitHub 上のマージは squash and merge のため、`git branch -d` では「HEAD にはマージされていない」という警告が出る。これは、リモート追跡ブランチ(`origin/feature/...`)がマージ済みと判定されるため、削除自体は成功する。`git fetch --prune` を先に実行すると追跡参照が消え、`-d` が失敗するので、必ず `-d` を先に実行する。`-D` は使わない。

## Git 操作
- ユーザーが明示的に依頼するまで、コミット・プッシュ・PR作成を行わない。
- ファイルを編集した後は、変更内容の報告で止める。
- ステージングは `git add .` ではなく、ファイルを指定して行う。
- Issue は使わない。コミットメッセージと PR に Issue 番号は付けない。
- コミットメッセージと PR のタイトル・本文は、日本語で変更内容を簡潔に書く。
- コミットの依頼を受けたら、README.md の「開発履歴」に追記する行(日付・バージョン・説明)を提案し、ユーザーの了承を得てから、同じコミットに含める。

## Shopify CLI
- `shopify` コマンド(`auth`、`theme pull`、`theme dev`、`theme push`、`theme delete` など)は実行しない。ユーザーが実行する。
- 調査や修正にストア側の情報(テーマエディタの設定、アプリ、エラー内容、再現 URL など)が必要な場合は、ユーザーに提供を求める。リポジトリのコードだけでは判断できない点は、推測せずその旨を報告する。
